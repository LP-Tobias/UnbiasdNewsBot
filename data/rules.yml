# data/rules.yml
version: "3.1"
rules:

- rule: Default Fallback
  steps:
    - intent: nlu_fallback
    - action: action_default_fallback

- rule: Summarize news rule
  condition:
  - active_loop: null
  steps:
    - intent: summarize_news
    - action: action_summarize_news
    - action: action_debias_summary
    - action: action_perspective_feedback
